BEGIN

    INT a := 1;
    INT b, c := 2;
    b := 3;

    # Somatório das variáveis #
    INT sum := a + (b + c);

    bool checker := true;

    IF checker = true
    THEN checker = false
    FI;

END